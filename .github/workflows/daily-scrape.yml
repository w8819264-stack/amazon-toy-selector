name: Daily Amazon Toy Report

on:
  schedule:
    # 每天 UTC 0:00 运行（北京时间 8:00）
    - cron: '0 0 * * *'
  # 允许手动触发
  workflow_dispatch:

# 需要写入权限来推送变更
permissions:
  contents: write
  pages: write
  id-token: write

jobs:
  update-report:
    runs-on: ubuntu-latest
    timeout-minutes: 30

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: |
          pip install pandas openpyxl curl_cffi beautifulsoup4

      - name: Run daily pipeline
        run: python main.py

      - name: Commit & Push if changed
        run: |
          git config user.name "Amazon Bot"
          git config user.email "bot@amazon-toy-selector.local"
          # 使用 GITHUB_TOKEN 认证
          git remote set-url origin "https://x-access-token:${GITHUB_TOKEN}@github.com/${GITHUB_REPOSITORY}.git"
          git add index.html data/
          # 如果有变更才提交
          if git diff --cached --quiet; then
            echo "No changes to commit"
          else
            git commit -m "📊 Daily report update: $(date +'%Y-%m-%d')"
            git push origin HEAD
            echo "✅ Changes pushed!"
          fi
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
